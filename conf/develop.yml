---
    server:
        port: 8080
        feRoot: 'http://localhost:8848'

    database:

        dialect: sqlite

        # mysql:
        #     database: bundle_rocket
        #     username: root
        #     password: iI2sfyPuf74r70D9S9715Vvs
        #     port: 3306
        #     localhost: localhost

        sqlite:
            database: bundle_rocket
            storage: database/sqlite.db

    jwt:
        secret: bundle-rocket
        key: jwt
        duration: 3600
